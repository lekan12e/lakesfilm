import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const profile = () => {
  return (
    <SafeAreaView>
      <Text className="font-bold text-light-100">profile</Text>
    </SafeAreaView>
  )
}

export default profile